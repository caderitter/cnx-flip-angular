// component to display deck detail

import { Component } from '@angular/core';

@Component({
  selector: 'deck-detail',
  template: `<h1>Deck detail view</h1>`,
})

export class AppComponent  { name = 'OpenStax Flip - Deck Detail'; }
